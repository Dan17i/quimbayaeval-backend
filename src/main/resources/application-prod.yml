spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jpa:
    hibernate:
      ddl-auto: validate  # No crear/modificar tablas automáticamente en producción
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    show-sql: false
    open-in-view: false
  
  sql:
    init:
      mode: never  # No ejecutar scripts SQL automáticamente

# JWT Configuration
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

# CORS Configuration
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

# Server Configuration
server:
  port: ${SERVER_PORT:${PORT:8080}}
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
  error:
    include-message: always
    include-binding-errors: always

# Logging
logging:
  level:
    root: INFO
    com.quimbayaeval: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/quimbayaeval.log
    max-size: 10MB
    max-history: 30

# Cache Configuration
spring.cache:
  type: simple
  cache-names: evaluaciones,evaluacionesByCurso,cursos

# Actuator (para monitoreo)
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
